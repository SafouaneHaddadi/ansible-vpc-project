plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  tag:Environment: dev
  instance-state-name: running

keyed_groups:
  - key: tags.Role
    prefix: ""

hostnames:
  - instance-id

compose:
  ansible_host: instance_id